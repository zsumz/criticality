name: CI

on:
  pull_request:
  merge_group:
  push:
    branches: [main]

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

permissions:
  contents: read

env:
  CARGO_TERM_COLOR: always
  RUST_BACKTRACE: "1"

defaults:
  run:
    shell: bash

jobs:
  canonical:
    name: canonical
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    steps:
      - name: Check out source
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          persist-credentials: false
      - name: Set up Rust
        uses: ./.github/actions/setup-rust
      - name: Set up reviewed zrail
        uses: ./.github/actions/setup-zrail
      - name: Install zcheck
        run: cargo +1.96.1 install zcheck --version 0.0.1 --locked
      - name: Fetch locked dependency graph
        run: cargo fetch --locked
      - name: Run canonical zcheck qualification
        env:
          ZCHECK_CACHE_DIR: ${{ runner.temp }}/zcheck
          ZCHECK_EVIDENCE_DIR: ${{ runner.temp }}/zcheck-evidence
        run: >-
          zcheck run check
          --format github
          --logs-dir "$ZCHECK_EVIDENCE_DIR/logs"
          --receipt "$ZCHECK_EVIDENCE_DIR/receipt.json"
      - name: Upload zcheck evidence
        if: always()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
        with:
          name: zcheck-canonical
          path: ${{ runner.temp }}/zcheck-evidence
          if-no-files-found: error
